#include <stdio.h>
#include <stdlib.h>

int main()
{
    //Variable length arrays do not mean that you can modify the length of an array after you create it
    //a VLA keeps the same size that you create it with
    //It means you can use variables to specify the dimensions
}
